import request from "supertest";
const  {expect} = require('chai');


describe('Comment Create', () => {
    describe('Comment Create - Positive', () => {
        it('Comment create - all fields', (done) => {

                })

        })

        it('Comment create - required fields', () => {

        })

    describe("Comment create - Negative", () => {

        it('Comment create - all fields empty', () => {


        })

        it('Comment Create - non-required fields only', ()=> {


        })

    })

})
